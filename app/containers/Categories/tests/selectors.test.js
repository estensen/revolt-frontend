// import { selectCategoriesDomain } from '../selectors';
// import { fromJS } from 'immutable';

// const selector = selectCategoriesDomain();

describe('selectCategoriesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
